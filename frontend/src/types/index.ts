export enum ButtonType {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
}

export enum DataType {
  PERCENT = 'percent',
  STRING = 'string',
  MONEY_CHANGE = 'moneyChange',
  MONEY_RANGE = 'moneyRange',
  MONEY = 'money',
  NUMBER = 'number',
  URL = 'url',
  DATE = 'date',
  DATETIME = 'datetime',
}
